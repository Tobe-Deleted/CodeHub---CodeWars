using System;
​
public static class Kata
{
    public static int Quadrant(int x, int y)
    {
        if(x>0)
          if(y>0)
            return 1;
          else 
            return 4;
        
        if(x<0)
          if(y<0)
            return 3;
        return 2;
    }
}