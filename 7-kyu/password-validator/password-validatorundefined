using System;
using System.Linq;
​
public class PasswordValidator
{
  public static bool Password(string st)
  {
    return st.Any(char.IsUpper) && st.Any(char.IsLower) && st.Any(char.IsDigit) && st.Length > 7;
  }
}
​