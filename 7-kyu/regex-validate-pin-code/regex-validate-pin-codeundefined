using System;
using System.Linq;
using System.Text.RegularExpressions;
​
public class Kata
{
  public static bool ValidatePin(string pin)
    => pin.All(Char.IsDigit) && (pin.Length == 4 || pin.Length == 6) ? true : false;
}